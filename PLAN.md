# PLAN — 냉장고 체크인

- 근거 문서: `PRD.md` (충돌 시 `PRD.md`가 `prd_lite.md`보다 우선)
- 작성일: 2026-08-04

## 이번 사이클 목표

PRD에 정의된 두 핵심 기능 — ① 영수증 촬영 자동 등록, ② 임박 순 냉장고 목록 — 을 갖춘
모바일 세로 화면 웹앱을 완성해 Vercel에 배포한다.

## 성공 기준

- [ ] 가짜 품목 없이, 영수증 촬영만으로 `내 냉장고`에 품목이 등록된다 (직접 타이핑 없이)
- [ ] 앱 실행 후 목록이 2초 안에 뜬다
- [ ] 영수증 1장 처리(촬영→등록)가 10초를 넘지 않는다
- [ ] 소비기한은 표준 기준표 값으로 자동 계산되며, 시스템이 임의로 추정하는 경우는 없다 (기준표에 없는 품목은 사람이 직접 보관 일수를 입력해야만 등록된다)
- [ ] 같은 영수증을 두 번 등록하면 막힌다 (구매 일시 기준)
- [ ] 목록이 `지남 → 냉장 → 냉동 → 실온` 순, 각 칸 내부는 임박 순으로 정렬되고 색·숫자·막대가 함께 보인다
- [ ] 애매하거나 못 읽은 품목은 전부 `확인 필요`로 빠지고, 임의로 등록되지 않는다
- [ ] 커피 원두·영양제·세제 등 제외 카테고리 품목은 `확인 필요`를 거치지 않고 자동으로 걸러진다
- [ ] 이름이 같은 품목도 합쳐지지 않고 구매일시별로 별도 항목으로 남는다
- [ ] `.env` 키가 코드나 저장소에 노출되지 않은 채 Vercel에 배포된다

## 작업 순서 (먼저 되면 나머지가 쉬워지는 것부터)

> PRD 9장 개발 단위와 번호가 정확히 1:1은 아니다 — 여기서는 가짜 응답으로 완성하는 단계를 13번, 실제 AI 모델 교체를 17번으로 분리했다.

1. Next.js 프로젝트를 만들고 모바일 세로 레이아웃(기준 폭 390px)을 잡는다
2. 품목 데이터 모델(`id`/`등록 경로`/`품목명`/`수량`/`구매 일시`/`보관 구분`/`소비기한`)의 타입을 정한다
3. 품목 목록을 기기 로컬에 저장하고 읽어오는 함수를 만든다 (이름이 같아도 합치지 않고 별도 레코드로 저장, `id`로 구분)
4. 자주 사는 품목 30~50개로 소비기한 기준표(국내 우선, 없으면 USDA)를 만든다
5. 구매 일시와 기준 일수로 남은 일수·소비기한을 계산하는 함수를 만든다
6. 가짜 품목 몇 개로 목록 화면을 띄우고 임박 순으로 정렬한다
7. 목록을 `냉장`/`냉동`/`실온` 3개 구분으로 나눠 보여준다
8. 남은 기간에 따라 색(빨강/노랑/초록)·숫자·14일 기준 막대를 함께 표시한다 (14일 넘으면 막대 꽉 채움, 기한 지난 품목은 빈 막대)
9. 기한 지난 품목을 모으는 `지남` 칸을 목록 맨 위에 접이식으로 만들고, 개수는 접혀도 보이게 한다 (품목마다 원래 보관 구분을 함께 표시하고, 남은 품목과 시각적으로 구분되게 보여준다. 자동 삭제는 하지 않는다)
10. 품목 수량을 1씩 줄이는 기능과 통째로 삭제하는 기능을 만든다 (수량 0이면 목록에서 사라짐)
11. 목록 첫 표시가 2초를 넘지 않는지 확인하고 필요하면 줄인다
12. 영수증 촬영/사진첩 선택 화면을 만들고 `이어서 찍기`·`완료`로 여러 장을 한 묶음으로 처리한다
13. 영수증 이미지를 AI 모델에 넘겨 품목·수량·구매 일시를 받는 서버 API를 만든다 (처음엔 가짜 응답)
14. `완료` 시점에 구매 일시를 기존 등록 내역과 비교해 동일 영수증 중복 등록을 막는다
15. 인식된 품목을 제외 카테고리(커피 원두·차·향신료·영양제·건강기능식품·반려동물 사료·세제 등)부터 걸러 조용히 버리고, 남은 품목을 기준표와 대조해 식료품만 남기고 보관 구분·소비기한을 정한다 (수량이 찍히지 않은 품목은 1개로 본다)
16. 읽지 못했거나 기준표에 없는 품목은 `확인 목록`에 담아 보여주고, 하나씩 눌러 개별 화면에서 처리한다 — 이름을 고쳐 기준표에 매칭되면 자동 계산된 값으로, 그래도 없으면 사람이 보관구분·보관 일수를 직접 입력해 `확인`을 누르면 그 자리에서 바로 저장하고 목록으로 돌아간다(`아니오`/건너뛰기는 저장하지 않고 목록에서 제거). 목록이 비면 자동 저장분까지 합쳐 `이번 영수증 체크인` 요약 화면을 보여준다
17. 13번의 가짜 응답을 실제 AI 모델 호출로 교체한다
18. 16번 화면을 재사용해 영수증 없이 품목을 직접 추가하는 경로를 만든다
19. 영수증 1장당 인식 처리가 10초를 넘지 않는지 확인하고(이어서 찍은 장들도 각각 10초 기준), 처리 중임을 알리는 표시를 넣는다
20. 소비기한이 추정값이라는 안내, 영수증 사진이 외부로 전송된다는 안내, 앱을 지우거나 기기를 바꾸면 목록이 사라진다는 안내를 화면에 넣는다
21. `.env` 키를 코드에서 분리한 상태로 Vercel에 배포한다
