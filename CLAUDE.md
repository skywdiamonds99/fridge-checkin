# CLAUDE.md

이 파일은 `냉장고 체크인` 프로젝트에서 Claude Code가 지켜야 할 규칙서다.

## 프로젝트 개요

영수증 사진 한 장으로 냉장고 속 식료품과 남은 소비기한을 자동 관리하는 **모바일 세로 화면 전용 웹앱**.

- 사용자: 직접 장을 보는 1~2인 가구
- 해결하는 문제: 밖에서 냉장고에 뭐가 남았는지 확인할 수 없어 생기는 헛걸음
- 핵심 기능 2개: ① 영수증 촬영 자동 등록 ② 임박 순 정렬 목록
- 상세 기획은 `PRD.md`가 **유일한 기준**이다. `prd_lite.md`는 초안이므로 두 문서가 어긋나면 `PRD.md`를 따른다.
  (예: 직접 추가 기능은 `prd_lite.md`에서 비범위였으나 `PRD.md`에서 범위로 확정됨)

## 기술 스택

- **Next.js** (App Router) — 화면과 API를 한 프로젝트에서 처리
- **배포: Vercel**
- **저장소: 기기 로컬** (localStorage 등). 서버 DB 없음, 계정/로그인 없음
- **영수증 인식: 외부 AI 비전 모델 호출** (`.env`의 `OPENAI_API_KEY` 사용)
- 상태 관리 라이브러리, ORM, 백엔드 프레임워크는 도입하지 않는다. 3일 일정이므로 의존성을 늘리지 않는다.

**스택은 고정이다.** PRD에서 정한 Next.js를 쓰고, 배포는 Vercel을 쓴다. 다른 프레임워크로 바꾸거나 마이그레이션을 제안하지 않는다. (더 나은 대안이 떠오르더라도 제안하지 않는다 — 이미 내린 결정이다)

## 명령어

```bash
npm run dev
```

(프로젝트 초기화 전이면 `npx create-next-app@latest`로 현재 폴더에 생성한다. 기존 `PRD.md`, `.env`, `.bkit/`, `.claude/`를 덮어쓰지 않도록 주의)

## 절대 규칙 (위반 시 되돌릴 것)

1. **API 키를 코드에 넣지 않는다.** 반드시 `.env` 환경변수로만 읽는다. `.env`는 `.gitignore`에 있으므로 커밋되지 않아야 한다.
2. **AI 모델 호출은 서버 코드(Route Handler)에서만 한다.** 클라이언트 컴포넌트에서 키를 참조하지 않는다. (`NEXT_PUBLIC_` 접두사로 키를 노출시키지 말 것)
3. **영수증 원본 이미지를 저장하지 않는다.** 인식 요청 시점에만 전송하고 응답을 받으면 즉시 버린다. 서버에도 기기에도 남기지 않는다.
4. **저장하는 값은 5개로 한정한다.** `품목명`, `수량`, `구매 일시`, `보관 구분`, `소비기한`. 카드번호, 승인번호, 매장 정보, 결제 금액, 비식료품 품목명은 저장하지 않는다.
5. **소비기한을 임의 추정하지 않는다.** 기준표에 없거나 애매하면 반드시 `확인 필요`로 뺀다.
6. **비범위 기능을 만들지 않는다.** (아래 "만들지 않는 것" 참고) 요청받지 않은 기능을 선의로 추가하지 않는다.
7. **파일을 바로 삭제하지 않는다.** 지워야 할 파일은 프로젝트 안에 `trash-can/` 폴더를 만들어 그리로 **옮기기만** 한다. 실제 삭제는 작업이 끝난 뒤 사용자가 직접 확인하고 한다. (`trash-can/`도 `.gitignore`에 넣는다)
8. **새 파일은 `my-app` 폴더 안에만 만든다.** 바깥 경로(바탕화면, 홈 디렉터리, `/tmp` 등)에 파일을 만들지 않는다.

## 비밀 정보와 외부 서비스 인증

- `.env`와 `node_modules`는 `.gitignore`에 등록해 두고 **절대 커밋하지 않는다.** 새 비밀 정보 파일이 생기면 만들면서 바로 `.gitignore`에 추가한다.
- **토큰 값을 사용자에게 묻지 않고, 채팅에도 출력하지 않는다.** 필요한 값은 `.env`에서 읽어서 쓴다. 로그·에러 메시지에 값이 찍히지 않도록 주의한다.
- 외부 서비스 작업이 필요하면 해당 CLI를 설치하고 `.env`의 토큰으로 인증한다.

| 상황 | 방법 |
| --- | --- |
| Vercel 배포 등 | Vercel CLI 설치 → `.env`의 `VERCEL_TOKEN`으로 인증 |
| Supabase를 쓸 일이 생기면 | Supabase CLI 설치 → `.env`의 `SUPABASE_ACCESS_TOKEN`으로 작업 |
| GitHub 작업 | `.env`의 `GITHUB_TOKEN` 사용 |
| 영수증 인식 | 서버 코드에서 `.env`의 `OPENAI_API_KEY` 사용 |

- 다만 **배포·푸시처럼 외부에 영향을 주는 작업은 실행 전에 먼저 알리고 확인을 받는다.**

## 도메인 규칙

### 데이터 모델

품목 하나가 갖는 값:

| 필드 | 설명 |
| --- | --- |
| 품목명 | 영수증에 찍힌 그대로 (브랜드·제품명 포함) |
| 수량 | 영수증에 없으면 1 |
| 구매 일시 | 날짜 + **시각**. 영수증 중복 판별의 식별값 |
| 보관 구분 | `냉장` / `냉동` / `실온` 중 하나 (필수) |
| 소비기한 | `구매일 + 기준 일수`로 계산된 값 |

- 같은 이름의 품목이 이미 있어도 **합치지 않는다.** 구매일이 다르면 기한이 다르므로 별도 항목으로 담는다. 수량 합산은 같은 영수증 안에서만 일어난다.

### 소비기한 기준표

- `품목 + 보관 구분`을 키로 일수를 갖는다. 같은 육류도 냉장 3일 / 냉동 90일로 다르다.
- **국내 기준(식약처 소비기한 참고값)이 우선**, 국내에 없는 품목만 USDA FoodKeeper를 참고한다.
- 자주 사는 품목 30~50개로 시작한다. 처음부터 전부 채우려 하지 말고 `확인 필요`로 자주 빠지는 품목부터 넓힌다.

### 식료품 판별 경계

- **등록한다:** 사람이 먹는 식재료 전반, 주류(맥주·와인 등)
- **등록하지 않는다:** 커피 원두·차·향신료(기한이 개월 단위), 영양제·건강기능식품, 반려동물 사료·간식, 세제·봉투·생활용품
- 브랜드·제품명으로 가공 형태가 분명하면 그에 맞게 자동 처리한다. (`오뚜기 사골곰탕` = 레토르트 실온, `한우 국거리` = 생육 냉장)
- **조금이라도 불확실하면 `확인 필요`로 뺀다.** `확인 필요` 질문 개수에 상한을 두지 않는다. 등록이 길어지는 것이 틀린 목록보다 낫다.
- 글자를 못 읽은 줄은 버리지 않는다. 눈에 띄게 표시하고 사용자가 직접 적어 넣게 한다.

### 영수증 등록 흐름

- 촬영 화면에 `이어서 찍기` / `완료`를 둔다. 여러 장은 묶어 **한 번의 등록**으로 처리한다.
- **중복 검사는 `완료` 시점에 등록 단위로 한 번만** 한다. 장마다 검사하면 나눠 찍은 두 번째 장부터 막힌다.
- 구매 일시가 완전히 동일하면 `동일한 영수증을 이미 입력했습니다`로 안내하고 등록을 멈춘다.

### 목록 화면

- 순서: 맨 위 `지남` 칸 → `냉장` → `냉동` → `실온`
- 각 칸 내부 정렬은 **남은 기간 짧은 순** (사용자가 바꾸지 않는 한 유지)
- 상태 표시는 **색·숫자·막대 세 가지를 함께** 쓴다. 색만으로 구분하지 않는다 (색약·야외 가독성).
  - 3일 이하 빨강 / 4~7일 노랑 / 8일 이상 초록
  - 막대는 14일 기준으로 채우고 초과 시 꽉 참. 기한 지난 품목은 빈 막대
- `지남` 칸은 보관 구분과 무관하게 지난 품목을 모으고, 각 품목의 원래 보관 구분을 함께 표시한다.
- `지남 5개`처럼 접어두되 **개수는 접혀 있어도 항상 보인다.** (삭제 유도가 이 칸의 존재 이유)
- 기한이 지나도 자동 삭제하지 않는다. 삭제는 사용자만 한다.
- 수량을 1씩 줄일 수 있고, 0이 되면 목록에서 사라진다.

### 화면에 항상 표시할 안내

- 표시되는 소비기한은 **추정값**이며 제품에 적힌 날짜와 다를 수 있다.
- 영수증 사진이 인식을 위해 **외부로 전송**된다. (촬영 화면에 명시)
- 데이터는 기기 안에만 있으므로 **앱 삭제·기기 변경 시 목록이 사라진다.**

## UI 규칙

- **모바일 세로 전용, 기준 폭 390px.** PC 웹 대응은 하지 않는다.
- 한 손 조작이므로 목록 항목 터치 영역은 **최소 44px 높이**.
- 흰 배경 + 카드형 목록. 장식 최소화, 품목명과 남은 기간이 크고 또렷하게.
- 메인 색: 차분한 남색·청록 (`#2F6F6A` 또는 `#1F4E79`).
- **빨강·노랑·초록은 소비기한 상태 표시에만 쓴다.** 버튼·헤더 등 다른 용도로 쓰지 않는다.

## 성능 기준

| 항목 | 기준 |
| --- | --- |
| 목록 첫 표시 | 2초 이내 |
| 영수증 1장 처리 | 10초 이내 (처리 중 표시 필수) |

## 만들지 않는 것 (비범위)

- 소비기한 날짜 직접 수정
- 소비기한 임박 푸시 알림
- 레시피 추천
- 냉장고 공유 / 가족 계정 / 실시간 동기화
- 쇼핑 목록 / 장보기 알림
- 바코드 스캔 등록
- 데이터 백업·복원, 서버 DB, 로그인

## 개발 순서

**"눈으로 확인되는 것부터"** — 목록 화면을 먼저 만들고 영수증 쪽을 붙인다. 반대로 하면 인식이 완성될 때까지 아무것도 확인할 수 없다.

1. 프로젝트 생성 + 모바일 레이아웃 (1~2)
2. 로컬 저장/읽기 + 기준표 + 남은 일수 계산 (3~5)
3. 목록 화면: 정렬 → 보관 구분 → 색·숫자·막대 → `지남` 칸 → 수량/삭제 (6~11)
4. 영수증 촬영 화면 + 서버 인식 API (**먼저 가짜 응답으로 만들고** 흐름 완성 후 실제 모델 호출로 교체) (12~13)
5. 중복 검사 → 식료품 판별 → `확인 필요` 되묻기 → 등록 (14~17)
6. 직접 추가 (16번 화면 재사용) (18)
7. 성능 확인, 안내 문구, Vercel 배포 (19~21)

전체 21단계 상세는 `PRD.md` 9장 참고.

## 작업 방식

- **모든 설명과 주석은 한국어로 쓴다.** 응답, 코드 주석, 커밋 메시지, 문서 전부 해당된다. (변수명·함수명 등 코드 식별자는 영어를 써도 된다)
- **코드를 바꾸면 무엇을 왜 바꿨는지 한 줄로 알려준다.** 예: `목록 정렬을 남은 일수 기준으로 바꿈 — 기본 정렬 규칙이 임박 순이라서`
- 코드 작성 전, 해당 기능의 규칙이 `PRD.md`에 있는지 먼저 확인한다.
- 규칙이 없거나 서로 충돌하면 임의로 정하지 말고 물어본다.
- 한 번에 한 단계씩 만들고 눈으로 확인한 뒤 다음으로 넘어간다.
- **이미 설치된 서브에이전트를 적극 활용한다.** 설계 검증에는 `design-validator`, 설계-구현 차이 확인에는 `gap-detector`, 코드 품질 점검에는 `code-analyzer`, UI 구조 결정에는 `frontend-architect`, 보안 검토에는 `security-architect`를 부른다. 혼자 다 처리하려 하지 않는다.
