# 냉장고 체크인 서비스 기획서 (PRD)

- 작성자: 현지
- 작성일: 2026-08-03

---

## 1. 추진 배경 (문제 정의)

`냉장고 체크인`은 영수증 사진 한 장으로 냉장고 속 식료품과 남은 유통기한을 자동으로 관리해주는 앱이다.

주 사용자는 직접 장을 보는 1~2인 가구이며, 가족 단위 가구도 함께 쓸 수 있다.

이들이 겪는 문제는 단순하다. **집에 있는 줄 알고 안 샀는데 막상 없었을 때의 짜증**이다. 냉장고 안을 정확히 기억하는 사람은 없고, 확인하려면 집에 도착해야 한다. 하지만 필요한 정보는 이미 집에 도착하기 전, 마트에 서 있는 그 순간에 필요하다. 기억에 의존하는 한 이 문제는 반복된다.

`냉장고 체크인`은 밖에서도 냉장고에 뭐가 남았는지 정확히 확인할 수 있게 해서 이 짜증을 없앤다.

---

## 2. 현행 방식과 한계

현재 사람들이 냉장고 재고를 관리하는 방식과 그 한계는 다음과 같다.

| 현행 방식 | 한계 |
| --- | --- |
| 머릿속으로 기억하기 | 가장 흔한 방식이지만 가장 부정확하다. 며칠만 지나도 무엇이 남았는지 흐려지고, 정작 마트에서는 떠오르지 않는다. |
| 냉장고 문에 메모지 붙이기 | 집 안에서만 볼 수 있어 정작 필요한 순간(장 보는 중)에 무용지물이다. 꺼내 먹을 때마다 지우는 것도 잘 지켜지지 않는다. |
| 메모 앱이나 스프레드시트에 직접 적기 | 장을 볼 때마다 품목을 하나하나 입력해야 해서 며칠 안에 그만두게 된다. 소비기한은 따로 계산해 적어야 한다. |
| 기존 식재료 관리 앱 | 대부분 품목을 수동으로 등록해야 한다. 입력하는 수고가 얻는 이득보다 커서 결국 쓰지 않게 된다. |

공통된 한계는 **입력 비용**이다. 사람이 직접 적어야 하는 방식은 예외 없이 중단된다. 그리고 대부분의 방식이 **집 안에서만** 확인 가능하다는 점도 문제다.

`냉장고 체크인`은 이미 손에 쥐고 있는 영수증을 입력 수단으로 삼아 입력 비용을 없애고, 휴대폰으로 어디서든 확인할 수 있게 해서 두 한계를 동시에 푼다.

---

## 3. 목표와 기대효과

### 목표

- 장을 본 뒤 **영수증 촬영 한 번**으로 재고 등록이 끝나야 한다. 영수증으로 산 품목은 직접 타이핑할 일이 없어야 한다. 직접 입력은 영수증이 없는 식재료나 글자를 읽지 못한 줄을 채울 때만 쓰는 보조 수단이며, 기본 경로가 되어서는 안 된다.
- 앱을 열고 **3초 안에** "지금 뭐가 있고 뭘 먼저 먹어야 하는지"를 파악할 수 있어야 한다.
- 표시되는 소비기한은 임의 추정이 아니라 **공개된 표준 기준에 근거**해야 한다.

### 기대효과

- **헛걸음이 없어진다.** 마트에서 냉장고 상태를 확인할 수 있으므로, 있는 줄 알고 안 사는 상황과 없는 줄 알고 또 사는 상황이 함께 줄어든다.
- **버리는 음식이 줄어든다.** 소비기한이 임박한 순서로 보여주므로 무엇을 먼저 먹어야 할지가 자연스럽게 드러난다.
- **관리를 계속하게 된다.** 입력 비용이 거의 0이므로, 기존 방식들이 실패한 지점인 "며칠 쓰다 그만두기"를 넘어설 수 있다.

---

## 4. 주요 기능

핵심 기능은 2가지로 한정한다.

### 1) 영수증 촬영으로 식료품 자동 등록

- 설명: 영수증을 촬영하면 품목을 자동으로 읽어 `내 냉장고`에 담고, 품목별 표준 소비기한을 적용해 남은 기간을 자동으로 계산한다.
- 지켜야 할 규칙:
  - 영수증에서 인식한 품목 중 식료품만 등록한다. 경계는 다음과 같이 정한다.
    - **등록한다:** 사람이 먹는 식재료 전반, 그리고 주류(맥주·와인 등).
    - **등록하지 않는다:** 커피 원두·차·향신료(기한이 개월 단위라 임박 알림이 뜨지 않는다), 영양제·건강기능식품(식재료가 아니다), 반려동물 사료·간식, 세제·봉투·생활용품.
  - 소비기한 일수는 임의로 추정하지 않고, 공개된 표준 기준의 품목별 값을 근거로 사용한다. 계산식은 `구매일 + 기준 일수`로 고정한다.
  - 기준이 서로 다른 값을 줄 때는 국내 기준(식약처 소비기한 참고값)을 먼저 따르고, 국내 기준에 없는 품목만 해외 기준(USDA FoodKeeper)을 참고한다. 국내 마트 영수증을 읽는 앱이므로 국내 유통 환경에 맞춘 값이 우선이다.
  - 기준표는 장 볼 때마다 사는 품목 30~50개로 시작한다. 처음부터 모든 품목을 담으려 하지 않고, 쓰면서 `확인 필요`로 자주 빠지는 품목부터 채워 넓힌다.
  - 품목명은 영수증에 찍힌 그대로, 브랜드와 제품명까지 통째로 읽어 판단에 쓴다. 같은 `한우 국거리`라도 생육이면 냉장 3일이고 레토르트면 실온 1년이라 100배 넘게 차이 나는데, 이 둘을 가르는 단서가 대부분 브랜드·제품명에 들어 있기 때문이다. (예: `오뚜기 사골곰탕`은 레토르트 실온, `한우 국거리`는 생육 냉장)
  - 브랜드·제품명으로 가공 형태가 분명히 드러나면 묻지 않고 그에 맞는 보관 구분과 기준 일수를 적용한다. 다만 조금이라도 불확실하면 자동으로 정하지 말고 `확인 필요`로 뺀다.
  - 품목명을 못 읽었거나 식료품인지 애매하면 임의로 판단해 등록하지 말고, `확인 필요` 목록으로 빼서 사용자에게 물어본다. (식료품이 아닌 제품일 수 있기 때문이다.)
  - `확인 필요`로 묻는 질문 개수에는 상한을 두지 않는다. 질문이 많아 등록이 길어지더라도, 틀린 목록이 남는 것보다 낫다.
  - 기준표에 없는 품목도 마찬가지로 추정 등록하지 말고 `확인 필요`로 분류한다. 다만 `확인 필요`에서 사용자가 보관 구분과 보관 일수를 직접 입력하는 것은 허용한다 — 여기서 금지하는 "임의 추정"은 시스템(AI 포함)이 근거 없이 값을 만들어내는 것이며, 사용자가 스스로 판단해 입력한 값은 해당하지 않는다.
  - 영수증에 찍힌 수량을 함께 읽어 품목의 수량으로 담는다. 수량이 찍혀 있지 않으면 1개로 본다.
  - 이미 냉장고에 같은 이름의 품목이 있어도 합치지 않고 별도 항목으로 담는다. 산 날짜가 다르면 소비기한도 다르므로, 합치면 둘 중 하나의 기한이 사라져 버리기 때문이다. 따라서 수량은 같은 영수증 안에서만 합쳐진다.
  - 모든 품목은 `냉장`, `냉동`, `실온` 중 하나의 보관 구분을 가진다. 구분에 따라 기준 일수가 달라지므로(같은 육류도 냉장 3일, 냉동 90일) 표준 기준표의 보관 방법별 값을 그대로 적용한다.
  - 화면에 표시되는 날짜는 추정값이며 제품에 실제로 적힌 날짜와 다를 수 있음을 항상 함께 표시한다.
  - 긴 영수증은 한 화면에 다 안 들어오므로, 촬영 화면에 `이어서 찍기`와 `완료`를 둔다. `이어서 찍기`로 여러 장을 찍으면 그 장들을 묶어 한 번의 등록으로 처리한다.
  - 영수증에 인쇄된 구매 일시(날짜와 시각)를 함께 읽는다. 이미 등록한 영수증과 구매 일시가 완전히 같으면 같은 영수증으로 판단해 등록하지 않고, `동일한 영수증을 이미 입력했습니다`라고 안내한다. 같은 매장에서 연달아 계산하더라도 시각까지 초 단위로 같을 수는 없으므로, 구매 일시를 영수증의 식별값으로 쓴다.
  - 단, 중복 검사는 `완료`를 누른 뒤 등록 단위로 한 번만 한다. 한 영수증을 나눠 찍은 장들은 구매 일시가 당연히 같으므로, 장마다 검사하면 두 번째 장부터 막혀버리기 때문이다.
  - 글자를 읽지 못한 줄은 그냥 버리지 않는다. 인식 결과 화면에 해당 줄을 눈에 띄게 표시하고, 누르면 사용자가 직접 품목명을 적어 넣을 수 있게 한다. 감열지는 글자가 날아가기 쉬워 부분 인식이 흔하므로, 못 읽은 줄이 있다는 사실 자체를 사용자가 알아야 한다.
  - 영수증 1장을 처리하는 시간은 10초를 넘지 않는다.

### 2) 내 냉장고 목록 (소비 우선순위 순 정렬)

- 설명: 보유 중인 식료품을 소비기한이 임박한 순서대로 보여줘, 밖에서도 뭐가 남았고 뭘 먼저 먹어야 하는지 3초 안에 파악하게 한다.
- 지켜야 할 규칙:
  - 기본 정렬은 남은 기간이 짧은 순이며, 이 순서를 그대로 유지한다. (정렬을 사용자가 바꾸는 기능은 이번 범위에 없다)
  - 남은 기간에 따라 색으로 구분한다. 3일 이하는 빨강, 4~7일은 노랑, 8일 이상은 초록으로 표시한다.
  - 색에만 의존하지 않는다. 남은 일수를 숫자로 함께 적고, 길이로 보여주는 막대도 함께 둔다. 색·숫자·길이 세 가지가 같은 정보를 전달해야 색약이 있는 사용자나 밝은 야외에서도 읽을 수 있다.
  - 막대 길이는 남은 일수를 14일에 맞춰 채우고, 14일이 넘으면 꽉 찬 상태로 둔다. 이렇게 해야 막대가 짧은 품목은 반드시 빨간 품목이 되어 길이와 색이 서로 어긋나지 않는다. 기한이 지난 품목의 막대는 빈 상태로 둔다.
  - 같은 이름의 품목이 여러 줄로 나올 수 있다. 이때도 정렬 규칙은 같으므로 기한이 짧은 것이 위에 놓이며, 사용자는 위에 있는 것부터 먹으면 된다.
  - 품목마다 수량을 함께 보여준다. 목록에서는 수량을 숫자로만 보여주고, `체크아웃`을 눌러야 수량을 1씩 줄이거나 삭제할 수 있다. 실수로 목록을 스치듯 넘기다 수량이 바뀌는 일을 막기 위함이다. 수량이 0이 되면 그 품목은 목록에서 사라진다.
  - 기한이 지난 품목은 자동으로 삭제하지 않는다. 삭제는 사용자만 할 수 있다.
  - **목록은 보관 구분으로 나누지 않고 하나의 목록으로 보여주되, 정렬 순서는 항상 "기한이 지난 것 → 임박한 것" 순으로 고정한다.** 기한이 지난 품목은 보관 구분과 무관하게 항상 맨 위에 오래 지난 순으로 모이고, 그 아래로 남은 품목이 임박한 순으로 이어진다. 각 카드에는 보관 구분을 함께 표시한다. 냉장·냉동·실온 구분만 보고 싶으면 화면 상단 필터로 좁혀볼 수 있다.
  - 기한이 지난 품목은 목록에서 접어두지 않는다. 항상 펼쳐진 채 맨 위에 보여서, 사용자가 그 식료품을 실제로 버리고 삭제하도록 유도한다. 다만 시각적으로는 옅게 표시해 남은 품목과 구분한다.
  - 앱을 켠 뒤 목록이 화면에 뜨기까지 2초를 넘지 않는다.

---

## 5. 범위와 비범위

### 범위 (이번에 만든다)

- 영수증 촬영으로 식료품 자동 등록 (품목 인식, 식료품 판별, 보관 구분 지정, 소비기한 자동 계산)
- 인식이 애매한 품목을 사용자에게 되묻는 `확인 필요` 처리
- 같은 영수증을 두 번 등록하지 않도록 구매 일시로 걸러내는 처리
- 내 냉장고 목록 화면 (임박 순 정렬, 지난 품목 항상 최상단, 색 구분, 보관 구분 필터)
- 영수증 없이 품목을 직접 추가하기 (선물받은 식재료, 텃밭 채소 등)
- 품목별 수량 표시와 하나씩 줄이기
- 사용자가 직접 품목을 삭제하는 기능 (다 먹었거나 버렸을 때)
- 등록한 품목 데이터를 사용자의 기기 안(로컬)에 저장

### 비범위 (이번에 만들지 않는다)

- 소비기한 날짜를 사용자가 직접 수정하기 — 이번에는 표준 기준값으로만 계산한다.
- 소비기한 임박 푸시 알림
- 남은 재료로 만들 수 있는 레시피 추천
- 여러 사람이 하나의 냉장고를 공유하기 (가족 계정, 실시간 동기화)
- 쇼핑 목록 / 장보기 알림 기능
- 바코드 스캔으로 등록하기 — 이번에는 영수증 촬영 한 가지 방법만 쓴다.

---

## 6. 사용자와 이용 흐름

### 사용자

- **주 사용자:** 직접 장을 보는 1~2인 가구. 퇴근길이나 장 보는 중에 휴대폰으로 냉장고 상태를 확인한다.
- **함께 쓰는 사용자:** 가족 단위 가구. 다만 이번 범위에서는 계정 공유를 만들지 않으므로, 한 사람이 대표로 등록하고 확인하는 형태가 된다.

### 이용 흐름 1 — 장을 본 뒤 (등록)

1. 마트에서 장을 보고 영수증을 받는다.
2. 앱을 열고 영수증을 촬영한다. 화면에는 실시간 카메라 화면이 그대로 보인다.
3. 앱이 영수증 사진을 서버로 보내 품목·수량·구매 일시를 읽어온다. 이 과정에서 사진이 외부로 전송되며, 10초 이내에 끝난다.
4. 읽어온 구매 일시를 이미 등록한 영수증과 비교한다. 완전히 같으면 `동일한 영수증을 이미 입력했습니다`라고 안내하고 등록을 멈춘다.
5. **인식된 품목 전부(자동으로 보관 구분·소비기한이 정해진 것 포함)를 확인 화면에 띄운다.** 자동으로 맞춰진 품목은 이름을 그대로 보여주고 `수정하기`만 있는 압축된 형태로, 읽지 못했거나 기준표에 없는 품목은 이름 입력창이 열린 상태로 보여준다. 자동으로 정해진 값도 사람이 확인하기 전까지는 저장하지 않는다 — AI가 다르게 읽었는데 우연히 다른 기준표 항목에 맞아떨어져 잘못 저장되는 일을 막기 위함이다.
6. 사용자가 필요한 품목만 이름·보관 구분·수량을 고치고, 등록하고 싶지 않은 품목은 `취소`로 뺀다.
7. `체크인 확정`을 누르면 그 시점의 값 그대로 전부 한 번에 `내 냉장고`에 저장되고 등록이 끝난다.

### 이용 흐름 2 — 영수증 없이 (직접 추가)

1. 선물받은 과일이나 텃밭에서 딴 채소처럼 영수증이 없는 식재료를 넣을 때는 직접 추가를 쓴다.
2. 품목명과 수량, 보관 구분을 고르면 기준표의 일수를 적용해 소비기한이 계산된다. 구매일은 추가한 날로 본다.
3. 이 경로는 보조 수단이다. 영수증으로 산 것까지 손으로 넣기 시작하면 이 앱을 쓸 이유가 사라진다.

### 이용 흐름 3 — 밖에서 (확인)

1. 퇴근길이나 마트에서 앱을 연다.
2. 2초 안에 목록이 뜬다. 기한이 지난 품목이 항상 맨 위(오래 지난 순)에 오고, 그 아래로 남은 품목이 임박한 순으로 이어진다. 보관 구분은 카드마다 표시되며, 냉장·냉동·실온만 보고 싶으면 필터로 좁힐 수 있다.
3. 남은 재료와 먼저 먹어야 할 것을 확인하고, 무엇을 사야 할지 판단한다.

### 이용 흐름 4 — 다 먹은 뒤 (삭제)

1. 품목 카드의 `체크아웃`을 눌러 수량 조절 상태로 들어간다. 재료를 하나 먹을 때마다 수량을 1씩 줄인다. 수량이 0이 되면 목록에서 사라진다.
2. 상한 음식처럼 통째로 버렸을 때는 `체크아웃` 후 수량과 상관없이 품목을 바로 삭제한다.
3. 기한이 지난 품목도 자동으로 사라지지 않으므로, 사용자가 직접 지워야 목록에서 없어진다.
4. 맨 위에 항상 펼쳐져 있는 지난 품목들이 이 삭제를 유도하는 장치다. 지난 품목이 눈에 띄게 쌓여 있으면 사용자가 냉장고를 열어 실제로 버리고, 그 김에 목록에서도 지우게 된다.

---

## 7. 기술 스택

- **Next.js** — 화면과 서버 쪽 코드(API)를 한 프로젝트에서 처리한다.
- **고정 이유:** Day 1 실습에서 쓴 것과 같은 구성이고, 3일차에 Vercel로 배포할 예정이라 그대로 이어지게 하기 위함이다.

### 영수증 인식

Next.js가 대신해주지 않는 부분은 영수증 이미지에서 글자를 뽑는 일 하나다. 이 부분은 **영수증 사진을 OpenAI의 비전 지원 모델에 그대로 넘겨 품목·수량·구매 일시를 받아오는 방식**으로 처리한다. 글자 읽기와 식료품 판별을 한 번에 처리할 수 있어 만들 것이 가장 적고, 3일 일정에 맞기 때문이다. 모델 호출은 서버 쪽 코드에서만 하고, 키는 환경변수로 넣는다.

---

## 8. 보안·개인정보 검토

### 다루는 정보

이 앱이 다루는 핵심 입력은 **영수증 사진**이다. 영수증에는 구입한 식료품 외에도 개인정보로 볼 수 있는 정보가 함께 찍힌다.

- 결제 카드번호 일부(마스킹된 뒷자리), 승인번호
- 방문한 매장명과 지점, 구매 일시
- 구매 품목 전체 (식료품이 아닌 것 포함)

특히 **매장·일시·품목이 모여 있으면 생활 반경과 생활 패턴이 드러난다.** 식료품 목록만 필요한 앱이 그보다 훨씬 많은 정보를 받게 되는 구조이므로, 받은 것을 그대로 쌓아두지 않는 것이 이 앱의 핵심 원칙이 되어야 한다.

### 지켜야 할 원칙

1. **데이터는 기기 안에만 둔다.** 등록된 품목 목록은 사용자의 기기(로컬)에 저장하고 서버 데이터베이스에 올리지 않는다. 계정과 로그인을 만들지 않으므로 수집하는 개인 식별 정보 자체가 없다. **단 하나의 예외는 영수증 사진이다.** 품목을 읽으려면 사진을 외부 AI 모델로 보내야 하므로, 이 순간만은 데이터가 기기 밖으로 나간다. 이 사실을 사용자에게 숨기지 않고 촬영 화면에 알린다.
2. **필요한 것만 남긴다.** 영수증에서 뽑아 저장하는 값은 `품목명`, `수량`, `구매 일시(날짜와 시각)`, `보관 구분`, `계산된 소비기한`으로 한정한다. 날짜뿐 아니라 시각까지 남기는 이유는 같은 영수증을 두 번 등록하는 것을 걸러내기 위해서다. 카드번호, 승인번호, 매장 정보, 결제 금액은 저장하지 않는다. **개인정보가 아닌 내부 관리용 값 2개(레코드 식별자 `id`, 등록 경로(영수증/직접))는 예외로 함께 저장할 수 있다.** 이름이 같은 품목을 구매일시별로 구분해 저장하고, 중복 검사 대상을 영수증 등록분으로 정확히 가르기 위해 필요하다.
3. **원본 사진은 남기지 않는다.** 영수증 이미지는 인식에 필요한 순간에만 보내고, 결과를 받으면 즉시 버린다. 우리 서버에도 사용자 기기에도 보관하지 않는다. 다시 볼 일이 있으면 사용자가 다시 찍으면 된다.
4. **비식료품은 버린다.** 인식 과정에서 걸러낸 비식료품 품목명은 저장하지 않는다. 무엇을 샀는지가 아니라 무엇이 냉장고에 있는지만 남긴다.
5. **API 키는 코드에 넣지 않는다.** 외부 서비스 키는 `.env` 파일로 관리하고, `.gitignore`에 등록해 저장소에 올라가지 않게 한다. 배포 환경(Vercel)에서는 환경변수로 주입한다.
6. **삭제는 사용자 뜻대로.** 사용자가 품목을 지우면 실제로 지워져야 하고, 통계 목적으로 남겨두지 않는다.

### 이번 범위에서의 판단

- 데이터를 기기 안에만 두므로, 서버가 털려서 사용자 데이터가 새는 상황 자체가 성립하지 않는다. 대신 **앱을 지우거나 기기를 바꾸면 목록이 함께 사라진다.** 이번 범위에서는 백업과 복원을 만들지 않으므로, 이 점을 사용자가 알 수 있게 해야 한다.
- 계정 공유와 다중 사용자 기능을 만들지 않으므로, 다른 사람의 데이터가 섞일 위험은 이번 범위에 없다.
- 푸시 알림을 만들지 않으므로 기기 토큰을 수집할 일이 없다.
- 표시되는 소비기한은 추정값이며 실제 제품 표기와 다를 수 있다. 이 점은 화면에 항상 함께 표시해, 앱의 계산을 식품 안전에 대한 보장으로 오해하지 않게 한다.

---

## 9. 개발 단위

순서의 원칙은 **"눈으로 확인되는 것부터"**다. 목록 화면을 먼저 만들어 두면 가짜 데이터만 넣어도 앱이 동작하는지 눈으로 볼 수 있고, 그 뒤에 영수증 쪽을 붙이면 된다. 반대 순서로 하면 영수증 인식이 완성될 때까지 아무것도 확인할 수 없다.

1. Next.js 프로젝트를 만들고 모바일 세로 화면(기준 폭 390px) 기본 레이아웃을 잡는다.
2. 품목 하나가 가지는 값(`품목명`, `수량`, `구매 일시`, `보관 구분`, `소비기한`)의 형태를 정한다.
3. 품목 데이터를 기기 안(로컬)에 저장하고 다시 읽어오는 기능을 만든다.
4. 표준 소비기한 기준표를 30~50개 품목으로 만든다. `품목 + 보관 구분`을 묶어 일수를 갖게 하고, 국내 기준을 먼저 채운 뒤 없는 것만 해외 기준으로 메운다.
5. 구매 일시와 기준 일수로 남은 일수를 계산하는 기능을 만든다.
6. 가짜 품목 몇 개를 넣어 목록 화면에 뿌리고, 남은 기간이 짧은 순으로 정렬한다.
7. 목록에 보관 구분 필터(모두 보기/냉장/냉동/실온)를 붙인다. 필터와 무관하게 목록 자체는 하나의 평면 목록이다.
8. 남은 기간에 따라 색을 입히고(3일 이하 빨강, 4~7일 노랑, 8일 이상 초록), 남은 일수 숫자와 14일 기준 막대를 함께 붙인다.
9. 기한이 지난 품목이 항상 목록 맨 위(오래 지난 순)에 오도록 정렬 규칙을 만든다. 접어두지 않고 항상 펼쳐 보여주되, 시각적으로 옅게 표시해 구분한다.
10. `체크아웃` 버튼을 누르면 나타나는 수량 -1/삭제 기능을 만든다. (수량이 0이 되면 목록에서 사라진다)
11. 목록이 화면에 뜨기까지 2초를 넘지 않는지 확인하고, 넘으면 줄인다.
12. 영수증을 촬영하는 화면을 만든다. 뷰파인더에는 실제 카메라 화면이 실시간으로 보여야 한다(은행 카드 스캔 화면처럼). 카메라를 쓸 수 없으면 사진첩에서 고르는 방식으로 대신한다. `이어서 찍기`와 `완료`로 여러 장을 한 번의 등록으로 묶는다.
13. 영수증 사진을 AI 모델에 넘겨 품목·수량·구매 일시를 받아오는 부분을 서버 쪽 코드로 만든다. 먼저 가짜 결과를 돌려주게 해두고 14~17번을 완성한 뒤, 실제 모델 호출로 바꾼다.
14. `완료`를 누른 시점에 구매 일시를 이미 등록된 영수증과 비교해, 같으면 `동일한 영수증을 이미 입력했습니다`라고 안내하고 등록을 멈춘다. (나눠 찍은 장들은 한 묶음이므로 서로 중복으로 보지 않는다)
15. 읽어온 품목을 기준표와 대조해 식료품만 남기고, 보관 구분과 소비기한을 정한다. (아직 저장하지 않는다)
16. 자동으로 정해진 품목과 못 읽었거나 애매한 품목을 모두 한 확인 화면에 띄운다. 자동으로 정해진 건 압축된 형태(이름 고정 + 수정하기)로, 애매한 건 펼친 형태(입력창 + 취소 + 수정하기)로 보여준다.
17. `체크인 확정`을 누른 시점의 값 그대로 전부 한 번에 `내 냉장고`에 저장하고, 목록 화면에 반영한다.
18. 16번의 직접 입력 화면을 재사용해, 영수증 없이 품목을 추가하는 경로를 만든다. (품목명·수량·보관 구분을 고르면 기준표로 소비기한을 계산하고, 구매일은 추가한 날로 본다)
19. 영수증 1장 처리가 10초를 넘지 않는지 확인하고, 처리 중임을 알리는 표시를 넣는다.
20. 표시되는 날짜가 추정값이라는 안내와, 영수증 사진이 인식을 위해 외부로 전송된다는 안내를 화면에 넣는다.
21. Vercel에 배포하고, API 키는 코드가 아닌 환경변수로 넣는다.
