# DESIGN — 냉장고 체크인

- 근거 문서: `PLAN.md`, `PRD.md`
- 작성일: 2026-08-04
- **시각 디자인 기준**: [Figma](https://www.figma.com/design/VUxHw5vxqKIyANJnnfJaRw/) (Main Home / 냉장고 둘러보기 / 냉장고 체크인 / 체크인 내용물 확인하기 4개 프레임). Figma 파일은 읽기 전용 참고 자료이며 절대 수정하지 않는다. 아래 색상·구조는 이 Figma를 코드로 옮긴 결과다.

## 색상 토큰 (Figma 기준)

| 변수 | 값 | 용도 |
| --- | --- | --- |
| `--color-brand` | `#55650d` | 버튼·선택 텍스트 (진한 올리브) |
| `--color-brand-light` | `#bdd06f` | CTA 배경 (연한 올리브) |
| `--color-bg` | `#f8f9fa` | 화면 배경 |
| `--color-surface` / `-alt` / `-muted` | 흰색 / `#f3f4f5` / `#edeeef` | 카드·입력창 배경 |
| `--color-text-primary` / `-secondary` | `#191c1d` / `#46483a` | 본문 텍스트 |
| `--color-red` / `-yellow` / `-green` / `-expired` (+ `-bg`) | PRD 상태색 | 소비기한 상태 표시 전용 |

## 내비게이션

화면마다 있던 "← 뒤로" 헤더를 없애고, **하단 고정 탭바(홈 / 냉장고 둘러보기 / 체크인)**로 통일했다. `components/BottomNav.js`가 모든 화면에 상시 표시된다.

## 화면 구성

### 1. Main Home (`/`) — 대시보드, 앱을 열면 바로 보이는 화면

인사말 + 통계 벤토(영수증 체크인 CTA, 유통기한 임박 개수, 체크인 현황=총 수량) + 냉장고 파먹기 대기 중(가장 임박한 3개 미리보기 + 전체 보기→`/browse`) + 보관 위치 분류(냉장/냉동/실온 바로가기→`/browse?filter=`). Figma "Main Home" 그대로.

- 임박 개수는 3일 이하(빨강) 품목 수, 지난 품목은 제외하고 계산
- 미리보기 카드는 `/browse`의 목록 카드와 같은 시각 언어(아이콘 사각형 + 왼쪽 컬러바 + 상태색 텍스트)를 쓰되 수량 조작 버튼은 없음 (미리보기 전용)
- 계정이 없어 Figma의 "레몬님" 같은 이름 인사는 빼고, 시간대별 인사말만 사용

### 2. 냉장고 둘러보기 (`/browse`) — 전체 품목 목록

필터 칩(모두보기/냉장/냉동/실온) + 목록. `모두보기`는 PRD가 정한 순서(지남 → 냉장 → 냉동 → 실온)를 그대로 유지하고, 개별 칩을 선택하면 그 구분(및 그 구분의 지난 품목)만 보여준다.

- 정렬: 각 구분 안에서 남은 일수 짧은 순. `지남` 칸 내부는 오래 지난 순
- 색: 3일 이하 빨강 / 4~7일 노랑 / 8일 이상 초록 (색+숫자+막대 동시 표시), 카드 왼쪽 컬러바로도 표시
- 막대: 14일 기준으로 채움, 넘으면 꽉 찬 상태 유지. 기한 지난 품목은 빈 막대
- `지남` 카드에는 원래 보관 구분을 함께 표시. 기한이 지나도 자동 삭제하지 않으며, 삭제는 사용자만 한다
- 화면 폭 기준 390px, 카드 터치 영역 최소 44px
- 하단에 안내 문구 상시 표시: "표시되는 날짜는 추정값입니다" / "앱을 지우거나 기기를 바꾸면 목록이 사라집니다"

### 3. 냉장고 체크인 (`/receipt`) — 영수증 촬영

Figma의 카메라 뷰파인더 스타일(코너 브래킷 + 스캔 라인) + 원형 촬영 버튼 + "이어서 촬영하기"/"체크인 진행하기" 버튼.

- "이어서 촬영하기": 여러 장을 한 묶음으로 쌓음 (긴 영수증 대응). 촬영된 사진은 메모리에만 유지하고 기기에 저장하지 않는다
- "체크인 진행하기": 서버로 전송해 품목·수량·구매 일시를 읽어온 뒤(장당 최대 10초), 그 결과의 구매 일시로 중복 영수증 검사 실행. 응답을 받는 즉시 촬영분은 폐기한다

### 4. 체크인 내용물 확인하기 (`/receipt`, 확인 단계) — 자동 판별 안 된 품목만 한 화면에

Figma "체크인 내용물 확인하기"를 따라 **목록→개별 화면 2단계가 아니라, 확인이 필요한 품목 전부를 카드로 펼쳐놓고 한 화면에서 처리**한다.

- **자동 판별된 품목은 이 화면에 나타나지 않는다.** 인식 직후 사람 개입 없이 곧바로 저장된다
- 카드마다: 인식된 원문 이름, 수정 가능한 품목명 입력, 보관구분 선택, 수량 스테퍼, 소비기한 미리보기(자동 계산 또는 직접 입력), ✕(제외) 버튼
- 값이 바뀔 때마다 실시간으로 매칭을 다시 시도한다 — 이름을 고쳐 기준표에 걸리면 보관구분·소비기한이 자동으로 채워진다
- 그래도 기준표에 없으면, **사람이 보관구분과 보관 일수를 직접 입력**해서 등록할 수 있다. 기준표는 자주 쓰는 품목부터 시작해 계속 넓혀가는 초기 목록이므로(PRD 4장), 아직 없는 품목이라고 등록 자체를 막지는 않는다. 다만 이 값은 AI나 시스템이 추정한 게 아니라 **사람이 직접 판단해 넣은 값**이라는 점이 다르다 — "시스템이 임의로 추정하지 않는다"는 원칙은 지키면서도, 사람이 아는 값까지 막지는 않는다
- ✕(제외)는 저장하지 않고 카드를 목록에서 뺀다
- 하단 고정 "확인하고 추가하기" 버튼은 **남은 카드가 전부 준비 상태일 때만** 활성화되고, 누르는 순간 전부 한 번에 저장한 뒤 홈으로 이동한다 (카드별 개별 저장 버튼 없음)
- 질문 개수 제한 없음

### 5. 직접 추가 화면 (`/add`, 영수증 없이·보조 수단)

품목명 · 수량 · 보관구분 · 소비기한(자동 계산 또는 기준표에 없으면 직접 입력) 입력 폼. 4번 확인 카드와 입력 필드(`components/ItemFields.js`)를 그대로 공유한다.

- 구매일은 오늘 날짜로 고정

## 데이터 모델

품목 하나가 갖는 값 (기기 로컬 저장):

| 필드 | 타입 | 설명 |
| --- | --- | --- |
| `id` | 문자열 | 레코드 고유 식별자 (내부 관리용, 개인정보 아님) |
| `등록 경로` | `영수증` \| `직접` | 영수증 중복 검사 대상을 가르는 값 (직접 추가는 검사 제외) |
| `품목명` | 문자열 | 영수증에 찍힌 그대로 (브랜드·제품명 포함) |
| `수량` | 숫자 | 영수증에 없으면 1 |
| `구매 일시` | 날짜+시각 | 영수증 등록은 인식된 값, 직접 추가는 추가한 시각 |
| `보관 구분` | `냉장` \| `냉동` \| `실온` | 필수 |
| `소비기한` | 날짜 | `구매 일시 + 기준 일수`로 계산 |

- 이름이 같은 품목이 여러 개 있어도 **합치지 않는다.** 각각 별도 `id`로 저장하고, 수량 합산은 같은 영수증(같은 구매 일시) 안에서만 일어난다.
- 카드번호·승인번호·매장 정보·결제 금액·비식료품 품목명은 저장하지 않는다 (PRD 8장).

## 데이터 흐름

### 흐름 A — 영수증 등록 (핵심 경로)

```
[촬영] 사용자가 사진 1~N장 촬영
   ↓
[완료] 클라이언트 → 서버 API (/api/receipts)로 이미지 전송
   ↓
[서버] AI 비전 모델 호출 → 품목명·수량·구매일시 텍스트로 추출
   │     (이미지는 응답받는 즉시 버림 — 서버에도 사용자 기기에도 저장 안 함)
   ↓
[서버] 구매일시를 기존 로컬 데이터와 비교할 수 있도록 그대로 반환
   ↓
[클라이언트] 등록 경로가 `영수증`인 기존 항목만 대상으로, 받은 구매일시가 이미 저장된 값과 같으면
   → "동일한 영수증을 이미 입력했습니다" 안내, 중단
   ↓ (다르면 계속)
[클라이언트] 추출된 각 품목을 먼저 제외 카테고리와 대조
   ├─ 제외 대상(커피 원두·차·향신료·영양제·건강기능식품·반려동물 사료·세제 등) → 조용히 버림 (저장 안 함)
   └─ 제외 대상 아님 → 기준표와 대조 (품목명 + 브랜드·제품명으로 가공 형태 판단)
      ├─ 기준표에 매칭 → 소비기한 자동 계산 → 사람 개입 없이 그 자리에서 바로 저장 (`등록 경로=영수증`)
      └─ 매칭 불확실 / 기준표에 없음 / 글자 못 읽음 → "확인 목록"에 담김
   ↓
[확인 목록 화면] 사람이 하나씩 탭해 개별 확인 화면(3-A)으로 진입
   ↓
[개별 확인 화면] 이름을 고치면 기준표 재매칭을 즉시 시도
   ├─ 매칭됨 → 자동 계산된 소비기한으로 `확인` → 즉시 저장, 목록으로 복귀
   └─ 매칭 안 됨 → 사람이 보관구분·보관 일수를 직접 입력 후 `확인` → 즉시 저장, 목록으로 복귀
      (`아니오`/건너뛰기는 저장하지 않고 목록에서만 제거)
   ↓ (확인 목록이 빌 때까지 반복 — 이름이 같아도 합치지 않고 매번 별도 레코드로 저장)
[이번 영수증 체크인] 자동 저장분 + 확인 목록에서 처리한 항목을 모아 보여줌 → `확인` → 홈으로
   ↓
[홈 화면] 목록 다시 읽어와 임박 순으로 렌더링
```

### 흐름 B — 직접 추가 (보조 경로)

```
[입력] 품목명·수량·보관구분 선택
   ↓
[클라이언트] 기준표에서 "품목+보관구분" 일수 조회 → 오늘 날짜 + 일수 = 소비기한
   ↓
[로컬 저장] 즉시 기록 (서버 호출 없음, `등록 경로=직접`으로 표시 — 영수증 중복 검사 대상에서 제외됨)
   ↓
[홈 화면] 목록 갱신
```

### 흐름 C — 조회 / 수량 변경 / 삭제

```
[앱 실행] localStorage에서 전체 품목 읽기
   ↓
[가공] 지남/냉장/냉동/실온으로 분류 → 각 구분 내 임박 순 정렬
   ↓
[렌더링] 2초 이내 화면 표시
   ↓
[사용자 조작] 수량 -1 또는 삭제 버튼
   ↓
[로컬 저장 갱신] 수량 0이 되면 항목 제거, 삭제 버튼은 즉시 제거
   ↓
[홈 화면] 목록 다시 정렬해 보여줌
```

- 기한이 지난 품목은 자동으로 삭제되지 않는다. 삭제는 사용자가 직접 할 때만 이루어진다
- 서버 데이터베이스 없음. 모든 조회/변경은 브라우저 안에서 끝남 (영수증 인식 호출만 예외)

## 기술 선택

### 기본 (고정, 변경 제안 없음)

| 기술 | 역할 | 오늘 배포와의 관계 |
| --- | --- | --- |
| **Next.js (App Router)** | 화면(홈/촬영/확인/추가)과 서버 API(`/api/receipts`)를 한 프로젝트로 처리 | `npx create-next-app`으로 바로 시작, Vercel과 100% 호환 |
| **Vercel** | 배포처 | Next.js 프로젝트를 그대로 연결하면 별도 서버 설정 없이 배포됨 |

### 추가로 필요한 것 (최소한만 제안)

| 기술 | 왜 필요한가 | 쉬운 설명 (1줄) |
| --- | --- | --- |
| **OpenAI API** (`openai` 패키지) | 영수증 사진에서 품목·수량·구매일시를 읽어내는 유일한 방법. 이미 `.env`에 `OPENAI_API_KEY`가 있음 | 사진을 보내면 "뭘 샀는지" 글자로 읽어서 알려주는 AI 서비스예요. 서버 코드에서만 호출해요. |
| **Tailwind CSS** (create-next-app 생성 시 옵션 선택) | 색상·막대·카드 스타일을 오늘 안에 빠르게 완성하기 위함. 새로 설치하는 게 아니라 프로젝트 만들 때 체크박스 하나 켜는 것 | 미리 만들어진 스타일 조각(예: "빨간 글씨", "둥근 카드")을 이름만 붙여 바로 쓰는 도구예요. 디자인을 처음부터 그릴 필요가 없어져요. |
| **브라우저 내장 기능** (localStorage, `<input type="file" capture>`) | 로컬 저장과 카메라 촬영 모두 브라우저가 기본 제공 — 설치할 것 없음 | 카메라 앱을 따로 만들지 않아도 폰이 기본으로 갖고 있는 카메라·저장 기능을 그대로 빌려 써요. |

- 상태 관리 라이브러리(Redux, Zustand 등), ORM, 별도 백엔드는 추가하지 않는다 — Next.js API Route + localStorage로 충분하고, 오늘 배포 목표에 의존성을 늘릴 이유가 없다.

### 품목 매칭 전략

- 매칭 정확도를 높이기 위해 AI에는 품목명뿐 아니라 `기준표 카테고리 후보`와 `확신도`를 함께 요청한다. 확신도가 낮으면 자동 등록하지 않고 `확인 필요`로 보낸다.
- 기준표는 국내 기준(식약처 소비기한 참고값)을 우선 적용하고, 국내 기준에 없는 품목만 해외 기준(USDA FoodKeeper)을 사용한다.
