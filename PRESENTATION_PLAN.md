# 발표자료 계획서 — 냉장고 체크인

- 근거 문서: `PRD.md`, `PLAN.md`, `DESIGN.md`, 실제 구현(`app/`, `components/`)
- 작성일: 2026-08-04
- 용도: Figma(Slides)로 옮길 발표자료의 슬라이드별 내용 초안

---

## 1. 왜 이 앱을 제작하게 되었는가 (배경)

- 1~2인 가구는 직접 장을 보고, 냉장고 상태를 스스로 기억해야 한다.
- 문제는 단순하다: **"집에 있는 줄 알고 안 샀는데 없었을 때"**, 혹은 반대로 **"없는 줄 알고 또 샀을 때"**의 짜증.
- 이 정보가 필요한 순간은 집이 아니라 **마트에 서 있는 순간**인데, 냉장고를 열어봐야 알 수 있는 구조라 문제가 반복된다.
- 기존 대안(머릿속 기억, 메모지, 메모 앱, 식재료 관리 앱)은 전부 **입력 비용**이라는 같은 지점에서 실패한다 — 사람이 직접 하나하나 적어야 하는 방식은 며칠 안에 중단된다.
- `냉장고 체크인`은 이미 손에 쥔 **영수증 한 장**을 입력 수단으로 삼아 입력 비용을 없애자는 아이디어에서 출발했다.

## 2. 이 앱이 해결해주는 Pain Point

| Pain Point | 기존 방식의 한계 | 냉장고 체크인의 해법 |
| --- | --- | --- |
| 냉장고 안이 기억나지 않는다 | 머릿속 기억은 며칠 만에 흐려진다 | 영수증 촬영만으로 정확한 목록이 자동으로 남는다 |
| 확인이 필요한 순간(마트)에 확인할 수 없다 | 메모지는 집 안에서만 보인다 | 휴대폰 하나로 어디서든 목록 확인 |
| 입력이 귀찮아서 며칠 쓰다 그만둔다 | 수동 입력 앱은 입력 비용이 이득보다 크다 | 영수증 한 장 = 등록 끝, 직접 타이핑 없음 |
| 뭘 먼저 먹어야 할지 몰라 음식을 버린다 | 소비기한을 따로 계산해야 한다 | 임박 순 자동 정렬 + 색·숫자·막대로 3초 안에 파악 |
| 같은 걸 또 사거나, 있는 줄 알고 안 산다 | 확인 수단이 집 안에 갇혀 있다 | 밖에서도 확인 가능 → 헛걸음 자체가 줄어든다 |

## 3. 앱의 주요 기능

핵심 기능은 2가지로 한정한다 (PRD 4장).

### ① 영수증 촬영으로 식료품 자동 등록
- 영수증을 촬영하면 AI 비전 모델이 품목·수량·구매 일시를 읽는다.
- 식료품만 골라내고(주류 포함, 커피·영양제·생활용품 등은 제외), 표준 기준표로 소비기한을 자동 계산한다.
- 애매하거나 못 읽은 품목은 임의로 추정하지 않고 `확인 필요`로 사용자에게 되묻는다.
- 같은 영수증을 두 번 등록하지 못하도록 구매 일시로 중복을 걸러낸다.
- 영수증 1장 처리 10초 이내.

### ② 임박 순 냉장고 목록
- 기한이 지난 품목이 항상 맨 위(오래 지난 순), 그 아래로 임박한 순 정렬.
- 색(빨강/노랑/초록) · 숫자(남은 일수) · 막대(14일 기준) 세 가지를 함께 표시해 색약·야외 환경에서도 읽힌다.
- 냉장/냉동/실온 필터로 좁혀볼 수 있다.
- `체크아웃`을 눌러야만 수량을 줄이거나 삭제할 수 있어 실수 조작을 막는다.
- 앱 실행 후 목록 표시 2초 이내.

### (보조) 영수증 없이 직접 추가
- 선물 받은 식재료, 텃밭 채소 등 영수증이 없는 경우를 위한 보조 입력 경로.

## 4. 각 페이지 안내

| 페이지 | 경로 | 역할 |
| --- | --- | --- |
| 홈 (Main Home) | `/` | 앱을 열면 바로 보이는 대시보드. 인사말, 영수증 체크인 바로가기, 유통기한 임박 개수, 체크인 현황(총 수량), 가장 임박한 3개 미리보기, 보관 위치(냉장/냉동/실온) 바로가기 |
| 냉장고 둘러보기 | `/browse` | 전체 품목을 하나의 목록으로 보여준다. 필터 칩(모두/냉장/냉동/실온), 지난 품목 항상 맨 위, 색·숫자·막대 상태 표시, `체크아웃`으로 수량 조절·삭제 |
| 냉장고 체크인 — 촬영 | `/receipt` (촬영 단계) | 실시간 카메라 뷰파인더로 영수증을 촬영. 여러 장은 `이어서 촬영하기`로 한 묶음 처리, `체크인 진행하기`로 서버 인식 요청 |
| 체크인 확인하기 | `/receipt` (확인 단계) | 인식된 품목 전부를 한 화면에서 검토. 자동 매칭은 압축 카드, 애매한 품목은 펼친 입력 카드로 시작. `체크인 확정`을 눌러야 한 번에 저장 |
| 직접 추가하기 | `/add` | 영수증 없이 품목명·수량·보관구분을 입력해 추가하는 보조 화면. 확인 화면과 입력 필드를 공유 |

## 5. 앱의 한계점

- **소비기한을 직접 수정할 수 없다.** 표준 기준값으로만 계산되고, 실제 제품에 적힌 날짜와 다를 수 있다.
- **임박 알림(푸시)이 없다.** 사용자가 직접 앱을 열어 확인해야 한다.
- **레시피 추천 기능이 없다.** 남은 재료로 무엇을 만들지는 알려주지 않는다.
- **가족·공동 사용이 안 된다.** 계정/로그인이 없어 한 사람이 대표로 등록·확인하는 형태로만 쓸 수 있다.
- **쇼핑 목록·장보기 알림이 없다.** 무엇을 사야 할지는 사용자가 목록을 보고 직접 판단해야 한다.
- **바코드 스캔 등록이 없다.** 등록 수단은 영수증 촬영 한 가지뿐이다.
- **데이터 백업·복원이 없다.** 모든 데이터가 기기 로컬에만 저장되므로, 앱을 지우거나 기기를 바꾸면 목록이 사라진다.
- **정렬 순서를 바꿀 수 없다.** 항상 "지난 것 → 임박한 순" 고정이며, 사용자가 다른 기준으로 정렬할 수 없다.
- **소비기한 기준표가 아직 30~50개 품목 수준.** 기준표에 없는 품목은 사람이 직접 보관 일수를 입력해야 등록된다.

---

## 발표 슬라이드 구성안 (총 10장)

Figma Slides로 옮길 때 아래 순서·분량을 기준으로 한다.

| # | 슬라이드 | 내용 |
| --- | --- | --- |
| 1 | 표지 | 앱 이름 `냉장고 체크인`, 한 줄 소개, 발표자 |
| 2 | 제작 배경 | 위 1번 내용 요약 (문제 상황 한 문장 + 기존 방식 한계) |
| 3 | Pain Point | 위 2번 표를 시각화 (한 화면에 3~4개 카드) |
| 4 | 핵심 기능 개요 | 2대 핵심 기능(영수증 자동 등록 / 임박 순 목록)을 아이콘+한 줄 설명으로 |
| 5 | 기능 상세 ① | 영수증 자동 등록 흐름 (촬영→인식→확인→저장, 실제 화면 스크린샷 포함) |
| 6 | 기능 상세 ② | 임박 순 목록의 색·숫자·막대 표시 방식 (실제 화면 스크린샷 포함) |
| 7 | 페이지 투어 ①② | 홈 / 냉장고 둘러보기 화면 소개 |
| 8 | 페이지 투어 ③④⑤ | 영수증 촬영 / 체크인 확인 / 직접 추가 화면 소개 |
| 9 | 한계점 | 위 5번 목록 중 핵심 4~5개만 추려 정리 |
| 10 | 마무리 | 배포 링크(Vercel), Q&A |

> 5·7·8번 슬라이드에는 실제 앱 화면 스크린샷을 넣는 것을 권장한다. Figma 디자인 시안(DESIGN.md에 링크된 파일) 또는 실행 중인 앱을 캡처해서 쓸 수 있다.
